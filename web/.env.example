# STEP SCHOOL KIDS — web
# Copy to .env.local for local development. Never commit real values.

# Trial-lesson form → Telegram forwarding (see src/app/api/trial/route.ts).
# Leave empty until a bot + chat are ready; the form then shows call/Telegram fallbacks.
TRIAL_TELEGRAM_BOT_TOKEN=
TRIAL_TELEGRAM_CHAT_ID=
